{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "rm -rf node_modules/.prisma node_modules/@prisma && prisma generate && npm install @prisma/client --legacy-peer-deps && npm run build"
}
